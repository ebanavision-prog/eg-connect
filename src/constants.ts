import { Contact, SuggestedConnection, LocalContentOpportunity } from './types';

export const MOCK_CONTACTS: Contact[] = [];

export const SUGGESTED_CONNECTIONS: SuggestedConnection[] = [];

export const MOCK_LOCAL_CONTENT: LocalContentOpportunity[] = [];
